// $Id$

project: boost_filesystem, boost_regex, boost_program_options, xsc_common {
  after += libXSC Utility
  libs  += XSC Utility

  includes += $(XSC_ROOT) $(XSC_ROOT)/contrib $(XSC_ROOT)/contrib/utility
  libpaths += $(XSC_ROOT)/lib
}
