// $Id$

project {
  after += Utility

  specific (prop:microsoft) {
    libs += Utilitys
  } else {
    libs += Utility
  }

  includes += $(XSC_ROOT)/contrib/CCF/utility
  libpaths += $(XSC_ROOT)/contrib/CCF/utility
}
