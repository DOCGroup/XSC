// $Id$

project {
  specific {
    CCF_ROOT = $(XSC_ROOT)/contrib/CCF/CCF/CCF
  }

  verbatim (gnuace, top) {
    CCF_ROOT = $(XSC_ROOT)/contrib/CCF/CCF/CCF
  }

  includes += $(XSC_ROOT)/contrib/CCF/CCF
}
